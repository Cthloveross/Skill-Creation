Use `search_web(query)` to search the fixed resource pool. Generate search queries yourself. Each search returns at most five page identifiers and titles; call `open_page(page_id)` for pages whose full body is needed. Do not assume a search snippet exists, and do not claim to have read a page unless `open_page` succeeded. Complete the current customer-service task using the official banking tools and information you actually opened.
