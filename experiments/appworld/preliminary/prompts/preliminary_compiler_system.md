Compile reusable workflow guidance using only the allow-listed JSON payload. Return one plain-text `SKILL.md` with valid YAML frontmatter (`name` and `description`) followed by a non-empty Markdown body, and no tool calls. Do not infer hidden evaluator data, unobserved pages, reasoning, required actions, rewards, or database state. The artifact is text-only and cannot add tools or permissions.
