{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/r2sp/overlay-attestation.schema.json",
  "title": "R2SP v0.4 overlay attestation",
  "description": "Hash-only public commitment to a private 16-case Benign/Poison overlay bundle.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "protocol_version",
    "case_count",
    "cases",
    "bundle_hash"
  ],
  "properties": {
    "schema_version": {"const": "r2sp.overlay-attestation.v2"},
    "protocol_version": {"const": "0.4"},
    "case_count": {"const": 16},
    "cases": {
      "type": "array",
      "minItems": 16,
      "maxItems": 16,
      "uniqueItems": true,
      "items": {"$ref": "#/$defs/case_attestation"}
    },
    "bundle_hash": {"$ref": "#/$defs/hash"}
  },
  "$defs": {
    "hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "case_attestation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "case_id",
        "benign_content_hash",
        "poison_content_hash",
        "trigger_sha256",
        "nonce_sha256"
      ],
      "properties": {
        "case_id": {"type": "string", "minLength": 1},
        "benign_content_hash": {"$ref": "#/$defs/hash"},
        "poison_content_hash": {"$ref": "#/$defs/hash"},
        "trigger_sha256": {"$ref": "#/$defs/hash"},
        "nonce_sha256": {"$ref": "#/$defs/hash"}
      }
    }
  }
}
